char* caesarCipher(char* s, int k) {
    int len = strlen(s);
    k = k % 26; 

    for (int i = 0; i < len; i++) {
        if (isalpha(s[i])) {
            char base = isupper(s[i]) ? 'A' : 'a';
            s[i] = ((s[i] - base + k) % 26) + base;
        }
    }
    return s;
}
